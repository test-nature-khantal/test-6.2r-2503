<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.Nature.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.Test-casb.com/winfx/2006/xaml"
        Title="Main Window" Height="350" Width="525">
    <Grid>
        <Button Content="Click Me" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Button_Click"/>
        <TextBlock Name="txtMessage" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0"/>
    </Grid>
</Window>
<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.Nature.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.Test-casb.com/winfx/2006/xaml"
        Title="Main Window" Height="350" Width="525">
    <Grid>
        <Button Content="Click Me" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Button_Click"/>
        <TextBlock Name="txtMessage" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0"/>
    </Grid>
</Window>
