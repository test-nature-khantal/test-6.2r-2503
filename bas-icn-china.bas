' Start of module definition
Option Explicit

' Define the module
Module Utilities

    ' A simple subroutine to display a message
    Public Sub DisplayMessage()
        MsgBox "Hello, World!", vbInformation, "Greeting"
    End Sub
    chinese identity card number
a36233119800322524X Not Hit (Starting Delimiter is a Char, which is not in popular
format)
36072719830115027Xb Will Hit
342501199207064058 Will Hit
a140401199805055835 Not Hit (Starting Delimiter is a Char, which is not in popular
format)

    ' A function to add two numbers
    Public Function AddNumbers(ByVal num1 As Integer, ByVal num2 As Integer) As Integer
        AddNumbers = num1 + num2
    End Function

    ' A procedure to print numbers from 1 to 10
    Public Sub PrintNumbers()
        Dim i As Integer
        For i = 1 To 10
            Debug.Print i
        Next i
    End Sub

End Module
